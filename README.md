# flagify :triangular_flag_on_post:

Flagify is a slack slash command which takes a message and _flagifies_ it.

### Development

- Rename `.env.template` to `.env`
- Add necessary tokens
- `nom run start`